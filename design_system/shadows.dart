import 'package:flutter/material.dart';

class AppShadows {
  static const m3ElevationLight1 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 1,
  );

  static const m3ElevationLight12 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const m3ElevationLight2 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 2),
    blurRadius: 6,
    spreadRadius: 2,
  );

  static const m3ElevationLight22 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const m3ElevationLight3 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const m3ElevationLight32 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 4),
    blurRadius: 8,
    spreadRadius: 3,
  );

  static const m3ElevationLight4 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 2),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const m3ElevationLight42 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 6),
    blurRadius: 10,
    spreadRadius: 4,
  );

  static const m3ElevationLight5 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 4),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const m3ElevationLight52 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 8),
    blurRadius: 12,
    spreadRadius: 6,
  );

  static const m3ElevationDark1 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const m3ElevationDark12 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 1,
  );

  static const m3ElevationDark2 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const m3ElevationDark22 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 2),
    blurRadius: 6,
    spreadRadius: 2,
  );

  static const m3ElevationDark3 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const m3ElevationDark32 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 4),
    blurRadius: 8,
    spreadRadius: 3,
  );

  static const m3ElevationDark4 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 2),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const m3ElevationDark42 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 6),
    blurRadius: 10,
    spreadRadius: 4,
  );

  static const m3ElevationDark5 = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 4),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const m3ElevationDark52 = BoxShadow(
    color: Color(0x26000000),
    offset: Offset(0, 8),
    blurRadius: 12,
    spreadRadius: 6,
  );

  AppShadows._();
}
